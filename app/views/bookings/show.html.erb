<h1> <%= @booking.item.name %> </h1>


<div class="card" style="width: 18rem;">
  <ul class="list-group list-group-flush">
    <li class="list-group-item"> <%= @booking.item.name %> </li>
    <li class="list-group-item"> <%= @booking.item.name %> </li>
    <li class="list-group-item"> <%= @booking.item.name %> </li>
  </ul>
</div>
